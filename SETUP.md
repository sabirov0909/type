# 🔧 Инструкция по настройке Firebase

## ⚠️ ВАЖНО! Если не работает регистрация

Скорее всего, в Firebase Console не включен метод авторизации через Email/Password. Следуйте этой инструкции:

## 📝 Пошаговая настройка

### Шаг 1: Откройте Firebase Console
1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Войдите в свой аккаунт Google
3. Выберите проект **"prince-10014"**

### Шаг 2: Включите Email/Password авторизацию
1. В левом меню нажмите **"Authentication"** (Аутентификация)
2. Перейдите на вкладку **"Sign-in method"** (Способ входа)
3. Найдите в списке **"Email/Password"**
4. Нажмите на него для редактирования
5. Включите переключатель **"Enable"** (Включить)
6. Нажмите **"Save"** (Сохранить)

### Шаг 3: Настройте правила безопасности для Realtime Database
1. В левом меню нажмите **"Realtime Database"**
2. Перейдите на вкладку **"Rules"** (Правила)
3. Замените существующие правила на эти:

```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('isAdmin').val() === true)",
        ".write": "auth != null && (auth.uid === $uid || root.child('users').child(auth.uid).child('isAdmin').val() === true)",
        "logins": {
          ".write": "auth != null && auth.uid === $uid"
        }
      }
    },
    "rooms": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
```

4. Нажмите **"Publish"** (Опубликовать)

### Шаг 4: (Опционально) Настройте домен
Если вы размещаете сайт на собственном домене:
1. Перейдите в **"Authentication"** → **"Settings"** (Настройки)
2. Во вкладке **"Authorized domains"** (Авторизованные домены)
3. Добавьте ваш домен в список

## 🎯 После настройки

1. Обновите страницу `auth.html` в браузере
2. Попробуйте зарегистрироваться снова
3. Если видите подробную ошибку - откройте консоль браузера (F12) для деталей

## 🐛 Отладка проблем

### Проблема: "Регистрация через email/пароль не включена"
**Решение:** Вернитесь к Шагу 2 и убедитесь, что Email/Password включен

### Проблема: "Permission denied" (Доступ запрещен)
**Решение:** Проверьте правила безопасности в Шаге 3

### Проблема: "Invalid API key"
**Решение:** Проверьте конфигурацию Firebase в файлах (apiKey должен быть правильным)

### Проблема: Ошибка сети
**Решение:** 
- Проверьте подключение к интернету
- Убедитесь, что Firebase не заблокирован файрволом
- Попробуйте отключить VPN/прокси

## 📊 Как сделать пользователя администратором

После успешной регистрации:

1. Откройте **Firebase Console** → **Realtime Database**
2. Найдите вашего пользователя в разделе `users`
3. Раскройте его данные
4. Найдите поле `isAdmin`
5. Измените значение с `false` на `true`
6. Теперь этот пользователь может зайти в `admin.html`

### Структура данных администратора:
```
users/
  └── USER_UID/
      ├── username: "ваше имя"
      ├── email: "email@example.com"
      ├── isAdmin: true  ← ИЗМЕНИТЕ ЭТО
      ├── createdAt: 1234567890
      ├── lastLogin: 1234567890
      └── logins/
          └── ...
```

## 🎮 Как проверить что всё работает

### Тест 1: Регистрация
1. Откройте `auth.html`
2. Нажмите "Зарегистрируйся"
3. Заполните форму:
   - Имя: минимум 2 символа
   - Email: правильный формат
   - Пароль: минимум 6 символов
4. Нажмите "Зарегистрироваться"
5. Должно появиться: "✓ Регистрация успешна!"
6. Вас перенаправит на `index.html`

### Тест 2: Вход
1. Откройте `auth.html`
2. Введите email и пароль
3. Нажмите "Войти"
4. Должно появиться: "✓ Успешный вход!"
5. Вас перенаправит на `index.html`

### Тест 3: Защита
1. Попробуйте открыть `index.html` без авторизации
2. Должно перенаправить на `auth.html`

### Тест 4: Админка (после установки прав)
1. Установите `isAdmin: true` для вашего пользователя
2. Откройте `admin.html`
3. Должны увидеть статистику и список пользователей

## 📞 Консоль браузера

Для отладки откройте консоль браузера (F12):
- В консоли будут показаны все этапы регистрации/входа
- Любые ошибки будут выведены с подробностями
- Это поможет понять где именно возникла проблема

## ✅ Чек-лист настройки

- [ ] Firebase Console открыт
- [ ] Проект "prince-10014" выбран
- [ ] Authentication → Email/Password включен
- [ ] Realtime Database правила настроены
- [ ] Страница обновлена в браузере
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Игра требует авторизацию
- [ ] Администратору установлен isAdmin: true
- [ ] Админ-панель работает

---

**Если проблемы остались:**
1. Откройте консоль браузера (F12)
2. Попробуйте зарегистрироваться
3. Скопируйте текст ошибки из консоли
4. Это поможет найти решение

**Успешной настройки! 🚀**
